/**
 * Additionne deux paramètres
 * @param a {number} Le premier nombre
 * @param b {number} Le deuxième nombre
 * @return {number} La somme
 */
const sum = (a, b) => a + b;

console.log(sum(1, 2));